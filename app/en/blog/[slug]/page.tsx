import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { ArticlePage } from "../../../components/ArticlePage";
import { getPost, legacyPostSlugs, posts } from "../../../lib/posts";
import { getLocalizedGuide } from "../../../lib/localized-guides";
import { absoluteUrl, localizedAlternates, ogImageUrl, postPath, schemaDate } from "../../../lib/site";

export const dynamicParams = false;
export function generateStaticParams() { return [...posts.map((post) => ({ slug: post.slug })), ...Object.keys(legacyPostSlugs).map((slug) => ({ slug }))]; }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const post = getPost(slug); if (!post) return {}; const canonicalSlug = post.slug; const title = `ByteQuant · ${post.title.en}`; const description = post.description.en.length >= 90 ? post.description.en : `${post.description.en} Follow the worked method, limitations, and verification steps before using the result.`; const localized = Boolean(getLocalizedGuide(canonicalSlug)); const alternates = localized ? { canonical: absoluteUrl(postPath("en", canonicalSlug)), languages: { "tr-TR": absoluteUrl(postPath("tr", canonicalSlug)), "en-US": absoluteUrl(postPath("en", canonicalSlug)), "de-DE": absoluteUrl(postPath("de", canonicalSlug)), "zh-CN": absoluteUrl(postPath("zh", canonicalSlug)), "x-default": absoluteUrl(postPath("en", canonicalSlug)) } } : localizedAlternates("en", postPath("tr", canonicalSlug), postPath("en", canonicalSlug)); return { title: post.title.en, description, keywords: ["privacy", "GDPR", "technical SEO", "GEO", "client-side tools", post.category.en], robots: slug === canonicalSlug ? undefined : { index: false, follow: true }, alternates, openGraph: { type: "article", siteName: "ByteQuant", locale: "en_US", alternateLocale: localized ? ["tr_TR", "de_DE", "zh_CN"] : "tr_TR", url: absoluteUrl(postPath("en", canonicalSlug)), title, description, publishedTime: schemaDate(post.date), modifiedTime: schemaDate(post.updated ?? post.date), authors: ["ByteQuant Editorial"], section: post.category.en, images: [{ url: ogImageUrl, width: 1200, height: 630, alt: title }] }, twitter: { card: "summary_large_image", title, description, images: [ogImageUrl] } }; }
export default async function EnglishArticle({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const post = getPost(slug); if (!post) notFound(); return <div lang="en"><ArticlePage post={post} locale="en" /></div>; }
